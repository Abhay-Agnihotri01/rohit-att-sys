# Database Configuration
import os

DB_CONFIG = {
    'host': 'localhost',
    'user': 'root',
    'password': '', # Default XAMPP/WAMP password is empty. Change if needed.
    'database': 'attendance_system'
}
